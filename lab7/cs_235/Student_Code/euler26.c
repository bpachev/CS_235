#include <stdlib.h>
#include <stdio.h>

int len_unit_cycle(int x);

int main()
{
  len_unit_cycle(7);
}

int len_unit_cycle(int x);
{
  int numer = 10;
  for (int i = 0; i < 10; i++)
  {
    d = numer / x;
    numer = 10 * (numer % x);
    printf("%d\n", d);
  }
}

